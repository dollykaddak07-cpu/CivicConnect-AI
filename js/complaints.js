document.addEventListener('DOMContentLoaded',async()=>{document.querySelectorAll('.filter-btn').forEach(b=>b.onclick=()=>{document.querySelectorAll('.filter-btn').forEach(x=>x.classList.remove('active-filter'));b.classList.add('active-filter');render(b.dataset.filter||'all')});try{window.liveComplaints=await CivicAPI.api('/api/complaints');render('all')}catch(e){window.liveComplaints=[];render('all')}});
function render(filter='all'){const tb=document.querySelector('#complaintsTable tbody');if(!tb)return;let a=window.liveComplaints||[];if(filter!=='all')a=a.filter(c=>key(c.status)===filter);tb.innerHTML=a.length?a.map(c=>`<tr data-id="${esc(c.id)}" style="cursor:pointer"><td><strong>#${esc(c.id)}</strong></td><td><div class="issue-cell"><div class="issue-icon"><i class="fa-solid ${icon(c.category)}"></i></div><div><strong>${esc(c.title)}</strong><span>${esc(c.location)}</span></div></div></td><td>${esc(c.category)}</td><td><span class="badge ${c.priority.toLowerCase()}">${esc(c.priority)}</span></td><td>${esc(c.department)}</td><td><span class="status-badge ${status(c.status)}"><span></span>${esc(c.status)}</span></td></tr>`).join(''):`<tr><td colspan="6" style="text-align:center;padding:50px">No complaints found.</td></tr>`;tb.querySelectorAll('tr[data-id]').forEach(r=>r.onclick=()=>{localStorage.setItem('selectedComplaintId',r.dataset.id);location.href='tracking.html'})}
function key(s){return s==='In Progress'?'progress':s.toLowerCase()}function status(s){return s==='Resolved'?'resolved':s==='In Progress'?'progress':'pending'}function icon(c){return /water/i.test(c)?'fa-droplet':/electric/i.test(c)?'fa-lightbulb':/sanitation|waste/i.test(c)?'fa-trash':/road|infrastructure/i.test(c)?'fa-road':'fa-circle-exclamation'}function esc(v){return String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[m]))}
